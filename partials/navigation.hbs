<div class="nav">
        {{#foreach navigation}}
            <a href="{{url absolute="true"}}">{{#if current}}>> {{/if}}{{label}}</a><br>
        {{/foreach}}
    <a class="subscribe-button icon-feed" href="{{@blog.url}}/rss/">Subscribe</a>
</div>
