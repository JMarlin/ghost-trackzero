<div class="nav">
        {{#foreach navigation}}
            {{#if current}}>> {{/if}}<a href="{{url absolute="true"}}">{{label}}</a>
        {{/foreach}}
    <a class="subscribe-button icon-feed" href="{{@blog.url}}/rss/">Subscribe</a>
</div>
